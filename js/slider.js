$(document).ready(function(){

  $('.slider-container').slick({
    infinite: true,
    dots: false,
    speed: 500,
    slidesToShow: 1,
    autoplay: true,
    autoplaySpeed: 2000,
    adaptiveHeight: true
  });
});